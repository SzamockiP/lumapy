// Edit me while the demo is running and save — the window updates on the next
// frame. This file is pulled in by shader.frag via #include, and the hot-reload
// watcher rebuilds any pipeline that depends on it.

vec3 tint() {
    return vec3(1.0, 0.9, 0.8);   // warm. Try vec3(0.8, 0.9, 1.0) for cool.
}
